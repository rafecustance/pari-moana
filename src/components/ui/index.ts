/**
 * UI COMPONENT CONVENTIONS
 *
 * 1. Keep components small and focused
 * 2. Use 'use client' only if the component uses hooks or event handlers
 * 3. Accept className prop for styling flexibility
 * 4. Use forwardRef for interactive elements (buttons, inputs)
 * 5. Keep animations in the animations/ directory, not here
 */

export { Button } from './Button';
export { ImagePlaceholder } from './ImagePlaceholder';

