/**
 * LAYOUT COMPONENT CONVENTIONS
 *
 * 1. Layout components handle structural concerns (header, footer, containers)
 * 2. Keep navigation state minimal
 * 3. Use 'use client' only when necessary (scroll detection, etc.)
 */

export { Navigation } from './Navigation';
export { Footer } from './Footer';

